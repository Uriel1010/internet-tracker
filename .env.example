# Interval (seconds) between connectivity checks
CHECK_INTERVAL=1
# Host to ping (DNS or HTTP HEAD)
TARGET_HOST=8.8.8.8
# Method: ping or http
CHECK_METHOD=ping
# Consecutive failures threshold before starting outage
FAIL_THRESHOLD=2
# Consecutive successes threshold before ending outage
RECOVER_THRESHOLD=2
# Optional: custom DB path (volume mount)
DB_PATH=app/data.sqlite3

# NOTE: If you see constant Offline results inside some containers/environments,
# switch to HTTP checking:
# CHECK_METHOD=http
# TARGET_HOST=example.com

# Optional: Webhook URL to receive outage events (start + end)
# When set, an HTTP POST JSON payload is sent on outage start (and end if ALERT_WEBHOOK_SEND_END=true)
# Payload includes event (outage.start/outage.end), service, outage_id, timestamps, duration (for end), target, method
ALERT_WEBHOOK_URL=https://n8n.urielmz.com/webhook-test/77cc4c77-1f99-415f-8ba6-e275effc04b7
# Whether to send outage start event (default false). Set true to also get start events.
ALERT_WEBHOOK_SEND_START=false
# Whether to also send outage end event (default true)
ALERT_WEBHOOK_SEND_END=true
# Request timeout seconds for webhook delivery (default 5)
ALERT_WEBHOOK_TIMEOUT=5

# Optional: periodic speedtest collection for Trends tab
# Requires speedtest-cli in the container/image
SPEEDTEST_ENABLED=false
# Seconds between runs (default 1800 = 30 minutes)
SPEEDTEST_INTERVAL=1800
# Service name bucket for speedtest samples
SPEEDTEST_SERVICE=default
